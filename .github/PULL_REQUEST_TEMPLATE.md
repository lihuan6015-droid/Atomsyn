## What

Brief description of the changes.

## Why

Motivation and context. Link related issues: `Closes #123`

## How

Key implementation details (if non-obvious).

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] `npm run test:cli` passes (if CLI changes)
- [ ] Tested on desktop app (`npm run tauri:dev`)
- [ ] New/modified JSON validates against schemas in `skills/schemas/`
- [ ] Screenshots attached (if UI changes)
