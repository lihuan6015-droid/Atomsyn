# Atomsyn V2.0 M5 · SLO 基线 (2026-04-10)

## CLI 性能

| 指标 | SLO 目标 | 实测 p95 | 状态 |
|---|---|---|---|
| `atomsyn-cli read` | < 3s | **113ms** | ✅ 远超 |
| `atomsyn-cli get` | < 3s | **94ms** | ✅ 远超 |
| `atomsyn-cli ingest --dry-run` | < 8s | ~120ms | ✅ 远超 (不含 LLM) |

> 注: ingest 的 SLO < 8s 包含 LLM 调用时间,dry-run 模式不调 LLM。
> 实际 ingest with LLM 取决于模型延迟,不在 CLI 层面测量。

## CLI 回归

| 指标 | SLO 目标 | 实测 | 状态 |
|---|---|---|---|
| CLI 回归通过率 | 100% | **19/19** | ✅ |

## 数据规模

| 维度 | 当前值 |
|---|---|
| 方法论 atoms | 125 |
| 经验碎片 | 15 (含 2 条 private) |
| 方法论骨架 | 1 (产品创新24步法) |

## 待用户验证

| 指标 | SLO 目标 | 验证方式 |
|---|---|---|
| GUI 首屏 | < 1.2s | 用户手动计时 |
| Memory Garden 渲染 | < 600ms | 用户手动观察 |
| Skill 触发命中率 | ≥ 95% | `scripts/test/skill-triggers.md` |
