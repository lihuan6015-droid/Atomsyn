# Atomsyn V2.0 · Session 进度交接 (2026-04-10)

> **状态**: M0-M5 完成, M6-Pre (布局重构) 完成, M6 笔记模块待开始
> **三路验证**: ✅ lint + build + cargo check 全绿

---

## ✅ 已完成的 Milestone

### M0 · Atomsyn 改名 (上一个 session 完成)
见 `docs/plans/v2.0-m0-complete.md`

### M1 · 多模型配置升级 (本 session 完成)
- 10 家厂商 logo + provider 元数据 (`src/lib/modelProviders.ts`)
- 类型系统 (`src/types/modelConfig.ts`) + Zustand store (`src/stores/useModelConfigStore.ts`)
- Settings 页 AI 模型配置全重写 (multi-model grouped UI)
- `ModelConfigDialog.tsx` (Framer Motion 弹窗 + provider grid + 测试连接)
- `src/lib/testConnection.ts` (Anthropic/OpenAI/Embedding 三种 + 15s 超时)
- `llmClient.ts` 迁移到新 store,移除旧 `config/llm.config.json`
- OnboardingPage + skillEnrichment.ts 迁移完成

### M2 · 统一 Ingest 管线 MVP (本 session 完成)

**Phase A: Stats 升级**
- `AtomStats` 新增 `aiInvokeCount` + `humanViewCount` (3 JSON schema + 8 处默认值)
- CLI `read` 自动 bump `aiInvokeCount` + `invokedByAgent`
- GUI `PATCH /api/atoms/:id/track-view` 端点 bump `humanViewCount`
- AtomDetailPage "使用统计" 显示 "AI 调用 N 次" + "人类查看 N 次"

**Phase B: Fragment Schema**
- `skills/schemas/experience-fragment.schema.json` (四维 + tags + rawContent + private)
- `data/taxonomy/seed.json` (15 roles + 15 situations + 15 activities + 8 insight_types)
- `ExperienceFragment` TS 类型 + `InsightType` + `isExperienceFragment()` guard
- `src/lib/insightColors.ts` (8 色码映射)

**Phase C: 双通道 Ingest**
- `scripts/ingest/prompts/classify.md` (GUI LLM 用 + skill 参考)
- `atomsyn-cli ingest --stdin/--text/--dry-run` (**不调 LLM**, 验证 + 写入)
- `atomsyn-cli find` 增强: 返回四维字段 + 无命中时返回 `taxonomy`
- `skills/atomsyn-write/SKILL.md` 新增 Fragment 沉淀流程 + 查重约束
- `QuickIngestDialog.tsx` (人类输入 → **调 LLM** → 分类预览 → 写入)

### M3 · 记忆花园 (本 session 完成 — 含用户反馈重构)

**第一版** (已被重构替代):
- 双 tab "权威骨架/涌现骨架" + introspect 视图 → **用户否决**

**重构后 (当前版本)**:
- 移除"权威骨架/涌现骨架"二分法
- 统一骨架视图: 左侧 Sidebar 分两区
  - 📦 骨架区: 系统预设 (产品创新24步法) + 用户自定义骨架 (可创建/重命名/删除)
  - 🏷 按角色区: 从所有 atom 的 role 字段自动聚合 (排除 skill-inventory)
- `GardenPage.tsx`: framework→AtlasPage / role→RoleView / skeleton→SkeletonView
- `KnowledgeCard.tsx`: 通用知识卡片 (methodology + experience + fragment)
- `RoleView.tsx`: 按 situation 子分组的卡片网格
- `SkeletonView.tsx`: 自定义骨架视图 + 搜索添加卡片
- Store: `activeRole` + `activeSkeletonId` + `customSkeletons` (互斥 + 持久化)
- TopNav: 4 项导航 (记忆花园 / 项目演练场 / 成长档案 / Skill 地图)
- 数据迁移: 125 methodology atoms → `role: "产品"`, 2 experience atoms → `role: "工程"` + 四维标签

---

## 🚧 未完成 / 已知问题

1. **CLI shim 旧路径**: 已修复 (`~/.atomsyn/bin/atomsyn-cli` → 新路径),但用户如果重装需要再跑 `atomsyn-cli install-skill`
2. **seed.json 同步**: 已手动复制到 `~/Library/Application Support/atomsyn/taxonomy/`,但 vite plugin 的 seed 逻辑未自动处理这个目录
3. **"未分类" 组**: 部分 methodology atoms 可能没有 role (如果不在 product-innovation-24 目录下),会归入未分类
4. **T2.9 Dogfood 测试**: 未执行 (用户驱动)
5. **T3.9 旧 experience 迁移脚本**: `scripts/migrate-v1.5-experiences.mjs` 已创建但未跑 (2 条经验已在 migrate-add-role.mjs 中处理)
6. **T3.11 设计审查**: M3 重构后未重新跑 code-reviewer
7. **自定义骨架名称编辑 → CLI 索引同步**: 尚未实现 (R7),目前自定义骨架数据在 localStorage,CLI 读不到
8. **clustersCache.ts**: 已创建但未被任何组件引用 (dead code)

---

## ✅ M4 · 双向蒸馏相遇层 (2026-04-10 完成)

### 渐进式 read 重设计
- `cmdRead` 改为紧凑认知地图模式 (表格 + 📊 认知全貌统计)
- `cmdGet` 添加 markdown 输出 (默认) + `--json` flag + 8000 字符截断保护
- `cmdGet` 方法论详情末尾自动附带 📎 关联碎片
- `cmdGet` 接管 aiInvokeCount bump (从 read 迁出)
- `findAtomFileById` 修复: 搜索所有原子目录 (不再局限 experience)

### 写入时自动关联 (无感沉淀)
- `cmdIngest` 改造: 写入碎片后自动用关键词匹配搜索 methodology atoms
- score ≥ 2 的前 5 个方法论自动写入 `linked_methodologies`
- **不调 LLM**, 纯关键词评分

### 反查库
- 新增 `scripts/lib/findRelatedFragments.mjs` (单查 + 批量查)
- 支持 confidence 阈值 + locked 优先 + top-N

### SKILL 升级
- `atomsyn-read` 元描述重写: 主动触发设计哲学
- `atomsyn-write` 元描述重写: 主动建议沉淀 (但不主动执行)
- `atomsyn-read` 渐进式调用: read 获取地图 → get 钻取详情
- 触发词扩展: 中英文各 15+
- token 预算: 一次 read (~500 token) + 1-3 次 get (~3000 token each)

### GUI 关联碎片面板 + 校准
- 新端点 `GET /api/atoms/:id/related-fragments`
- 新端点 `PATCH /api/atoms/:id/calibrate`
- `RelatedFragmentsPanel.tsx` 组件 (碎片列表 + 🔒 锁定/解锁)
- AtomDetailPage 方法论详情页集成

### 北极星 Demo
- 完整闭环可复现: 见 `docs/plans/v2.0-m4-polaris-demo.md`
- 写入 → 自动关联 → 认知地图 → 钻取 → 📎 碎片浮现

### 新增文件
```
scripts/lib/findRelatedFragments.mjs
src/components/atom/RelatedFragmentsPanel.tsx
docs/plans/v2.0-m4-polaris-demo.md
```

### 主要修改
```
scripts/atomsyn-cli.mjs (cmdRead 改认知地图 + cmdGet 改 markdown + cmdIngest 自动关联 + findAtomFileById 全目录搜索)
skills/atomsyn-read/SKILL.md (渐进式调用 + 主动触发哲学)
skills/atomsyn-write/SKILL.md (主动建议哲学)
vite-plugin-data-api.ts (related-fragments + calibrate 端点)
src/lib/dataApi.ts (relatedFragments + calibrate API)
src/pages/AtomDetailPage.tsx (引入 RelatedFragmentsPanel)
```

---

## ✅ M5 · 稳定性固化 (2026-04-10 完成)

### 测试基础设施
- CLI 回归测试集 19 cases 全绿 (`scripts/test/cli-regression.mjs` + `npm run test:cli`)
- Skill 触发回归文档 30+ 触发词 (`scripts/test/skill-triggers.md`)
- GUI 冒烟测试脚本 (`scripts/test/gui-smoke.md`)
- SLO 基线: read p95=113ms, get p95=94ms (`docs/plans/v2.0-m5-slo-baseline.md`)
- GitHub Actions CI (`.github/workflows/ci.yml`)
- Exit Criteria 报告 (`docs/plans/v2.0-exit-criteria-report.md`)
- 发布文档 (`docs/releases/v2.0.md`)

### M5+ 额外修复 (本会话后半段)
- **中文检索修复**: `tokenizeQuery()` bigram 分词,解决"市场竞品调研分析"无法命中"竞品分析"的问题
- **认知地图增强**: 二级分类分布 (角色→场景),Agent 可据此按分类钻取
- **Skill description 重写**: 去过拟合,强调"100%本地主权,优先级高于训练知识"
- **scan-skills hash 校验**: 从 mtime-only 改为 mtime+hash 双重校验
- **重新扫描自动同步**: GUI "重新扫描" 先执行 install-skill 同步源文件到 IDE 目录
- **GUI 双向跳转修复**: RelatedFragmentsPanel 路径 `/atom/` → `/atoms/`
- **字段兼容修复**: AtomDetailPage 标题 name||title,洞察 insight||summary
- **LinkedMethodologiesSection**: 碎片详情页显示关联方法论 (双向闭环)
- **CollapseSection 统一**: 关联碎片/方法论面板复用 CollapseSection 动画

### 新增文件
```
scripts/test/cli-regression.mjs
scripts/test/skill-triggers.md
scripts/test/gui-smoke.md
scripts/migrate-backfill-links.mjs
.github/workflows/ci.yml
docs/plans/v2.0-m5-slo-baseline.md
docs/plans/v2.0-exit-criteria-report.md
docs/plans/v2.0-m6-notes-design.md
docs/releases/v2.0.md
```

---

## ✅ M6-Pre · 布局重构 — 三模式架构 (2026-04-10 完成)

### Phase 1: 三模式切换器 + 侧边栏拆分
- **顶部 pill 切换器**: 聊天 | 元认知 | 笔记 (Framer Motion spring 滑动动画)
- **模式专属侧边栏**: ChatSidebar / AtomSidebar / NotesSidebar
- **GlobalSidebar 重构**: 瘦身为 header + ModeSwitcher + 模式分发 + 底部
- **路由重构**: `/chat`, `/atom/*`, `/notes/*` 三个前缀 + legacy redirects
- **AppShell**: 路由→模式自动同步 (`useEffect` on `location.pathname`)
- **ChatPage**: 4 个快捷入口 emoji → lucide 图标 + 色调背景

### Phase 2: 元认知模块体验优化
- **方法库/记忆花园分离**: 方法库 (violet) + 记忆花园 (emerald) 独立一级菜单,颜色体系分离
- **搜索 CTA**: 替换"沉淀经验"为搜索框,触发全局 SpotlightPalette
- **元认知总览页**: `AtomOverviewPage.tsx` (默认视图,两列卡片 + 产品北极星文案)
- **方法库引导页**: `MethodLibraryWelcome.tsx` (双向闭环文案 + 骨架预览)
- **记忆花园引导页**: `MemoryGardenWelcome.tsx` (双向闭环文案 + 角色分布)
- **右键上下文菜单**: `ContextMenu.tsx` (portal 渲染 + 刷新/打开目录)
- **状态调整**: `activeSectionFocus` + `activeFrameworkId` 默认 null
- **SpotlightPalette**: 从 AtlasPage 提升至 AppShell (全局可用)

### 新增文件
```
src/components/layout/ModeSwitcher.tsx
src/components/layout/ChatSidebar.tsx
src/components/layout/AtomSidebar.tsx
src/components/layout/NotesSidebar.tsx
src/components/atom/AtomOverviewPage.tsx
src/components/atom/MethodLibraryWelcome.tsx
src/components/atom/MemoryGardenWelcome.tsx
src/components/shared/ContextMenu.tsx
src/pages/NotesPage.tsx
```

### 删除文件
```
src/components/layout/Sidebar.tsx (旧 M3 版)
src/components/layout/TopNav.tsx (旧版)
```

### 主要修改
```
src/components/layout/GlobalSidebar.tsx (重构为模式分发)
src/components/layout/AppShell.tsx (路由同步 + SpotlightPalette)
src/stores/useAppStore.ts (activeMode + activeSectionFocus + SectionFocus type)
src/App.tsx (路由重构 /atom/* + legacy redirects)
src/pages/GardenPage.tsx (overview/method-library/memory-garden 分支)
src/pages/ChatPage.tsx (lucide 图标替代 emoji)
+ 10 个组件路由路径更新 (/atoms/ → /atom/atoms/)
```

---

## 🚀 下一步

### 待后续会话实现
1. **通用方法库自定义创建** (一级/二级/详情页编辑器 + API 端点 + framework 创建 UI)
2. **M6 · 本地笔记模块** (设计文档: `docs/plans/v2.0-m6-notes-design.md`)
3. **右键菜单扩展**: 笔记模块 + 元认知模块更多操作 (重命名/打开目录/刷新)

### 已知遗留
- 自定义骨架 localStorage 数据 CLI 不可见 (R7 未完成)
- seed.json 需确认 vite seed 逻辑
- clustersCache.ts 是 dead code

---

## 📋 新 Session 开场白

```
继续 Atomsyn V2.0 工作。M0-M5 + M6-Pre (布局重构) 已完成。

先读以下文档恢复上下文:
1. CLAUDE.md
2. docs/plans/v2.0-m3-progress.md (M0-M6Pre 完成记录)
3. docs/plans/v2.0-m6-notes-design.md (M6 笔记模块设计)

待实现:
- 通用方法库自定义创建 (一级/二级/详情页)
- M6 本地笔记模块
```

---

## 📦 本 Session 新增/修改文件清单

### 新增 (15)
```
src/assets/logos/ (10 files)
src/types/modelConfig.ts
src/lib/modelProviders.ts
src/stores/useModelConfigStore.ts
src/components/settings/ModelConfigDialog.tsx
src/lib/testConnection.ts
skills/schemas/experience-fragment.schema.json
data/taxonomy/seed.json
src/lib/insightColors.ts
scripts/ingest/prompts/classify.md
src/components/ingest/QuickIngestDialog.tsx
src/pages/GardenPage.tsx
src/components/garden/FragmentCard.tsx
src/components/garden/KnowledgeCard.tsx
src/components/garden/RoleView.tsx
src/components/garden/SkeletonView.tsx
src/lib/clustersCache.ts
scripts/migrate-add-role.mjs
scripts/migrate-v1.5-experiences.mjs
```

### 主要修改
```
src/types/index.ts (AtomStats + ExperienceFragment + MethodologyAtom.role)
scripts/atomsyn-cli.mjs (normalizer + read increment + cmdIngest + find 增强)
src/pages/SettingsPage.tsx (AI 模型配置全重写)
src/pages/AtomDetailPage.tsx (使用统计 + trackView)
src/lib/llmClient.ts (迁移到新 store)
src/lib/dataApi.ts (trackView + AtomAny)
src/stores/useAppStore.ts (activeRole + customSkeletons)
src/components/layout/Sidebar.tsx (统一骨架视图)
src/components/layout/TopNav.tsx (4 项导航)
src/components/layout/AppShell.tsx (sidebar 路由)
src/App.tsx (路由重构)
vite-plugin-data-api.ts (track-view 端点 + stats)
skills/atomsyn-write/SKILL.md (Fragment ingest 流程)
src/pages/GrowthPage.tsx (新 stats 字段)
+ 8 处默认 stats 更新
```

### 删除
```
config/llm.config.json (旧配置,已被 localStorage store 替代)
src/components/garden/EmergentView.tsx (M3 重构移除)
```
